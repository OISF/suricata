%YAML 1.1
---

outputs:
  - eve-log:
      enabled: true
      types:
        - alert
        - anomaly
        - files
        - flow
        - stats:
            enabled: no
        - zabbix:
            enabled: yes

app-layer:
  protocols:
    zabbix:
      enabled: true
